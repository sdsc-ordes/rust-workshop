// TODO: Extend `grades` with `grade` without using `push`.
//       Why does it work?
fn main() {
    // Extend a collection
    let grade: Option<&str> = Some("A+");
    let mut grades: Vec<&str> = vec!["B-", "C+", "D"];

    todo!();

    println!("{grades:?}")
}

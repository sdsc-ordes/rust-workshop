{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug Setup",
      "cargo": {
        "args": ["build", "--bin", "--package=setup"],
        "filter": {
          "kind": "bin"
        }
      },
      "args": []
    }
  ]
}
